<%= simple_form_for(@playlist) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: 'Playlist Name' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
